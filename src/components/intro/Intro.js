
import React from 'react';

import dublin from '../../resources/dublin.jpg';

const Intro = () => {
    return (
        <section id="#home">
            <img src={dublin} alt="Dublin" class="w-100 db black-10"/>
        </section>
    );
}

export default Intro;
